import style from './style.module.scss';

const FirstScreen = () => {
	return (
		<div className={style.container}>
			<div className={style.title}>First screen</div>
		</div>
	);
};

export default FirstScreen;
